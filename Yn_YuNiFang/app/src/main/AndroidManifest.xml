<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.yn_yunifang">

    <!-- if you want to load images from the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.NextActivity"></activity>
        <activity android:name=".activity.MoActivity"></activity>
        <activity android:name=".activity.ShangActivity"></activity>
        <activity android:name=".activity.ShangActivity1"></activity>
        <activity android:name=".activity.RegiesActivity"></activity>
        <activity android:name=".activity.LogActivity"></activity>
        <activity android:name=".activity.ZhiActivity"></activity>
<activity android:name=".activity.ZhiActivity1"></activity>
        <activity android:name=".activity.ZhiActivity_lie"></activity>

    </application>

</manifest>